import { ProfilepicPipe } from './profilepic.pipe';

describe('ProfilepicPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfilepicPipe();
    expect(pipe).toBeTruthy();
  });
});
