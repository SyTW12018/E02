language: node_js
node_js:
  - "stable"
dist: trusty
sudo: false

install:
- npm install -g @angular/cli

before_script:
- cd DIYHacks
- pwd
- ls -la
- npm install
cache:
  directories:
    - DIYHacks/node_modules

addons:
  chrome: stable

script:
  #- npm run lint
  - npm run test
  #- npm run build
  #- xvfb-run npm run e2e
